const feedDisplay = {
    display: 'flex',
    alignItems: 'center',
    flexDirection: 'column',
    width: '100%',
    height: '100vh'
};

const feedStyle = { feedDisplay };

export default feedStyle;